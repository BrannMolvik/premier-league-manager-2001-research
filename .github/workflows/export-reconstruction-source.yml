name: Temporary reconstruction source export

on:
  push:
    branches:
      - agent-runtime

jobs:
  export:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/upload-artifact@v4
        with:
          name: reconstruction-source
          path: reconstruction
          if-no-files-found: error
